# Gemini instructions

Repository conventions live in [`AGENTS.md`](./AGENTS.md) — read it before committing.
Branches are `<type>/GEN-<ticket>/<slug>`; commit subjects are
`[GEN-<n>] <type>(<scope>): <subject>`. Never invent a `GEN` number; never bypass hooks.

Code style and architecture: [`.claude/CLAUDE.md`](./.claude/CLAUDE.md).
